version: 2

sources:
  - name: PC_DBT_DB
    database: PC_DBT_DB
    schema: PC_DBT_SCHEMA
    tables:
      - name: ACCOUNT
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}        
        columns: 
        - name: ACCOUNT_ID
          tests:
          - unique
          - not_null
        
      - name: USER
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}        
        columns: 
        - name: USER_ID
          tests:
          - unique
          - not_null



  #         models:
  # - name: stg_customers
  #   columns: 
  #     - name: customer_id
  #       tests:
  #         - unique
  #         - not_null
  # - name: stg_orders
  #   columns: 
  #     - name: order_id
  #       tests:
  #         - unique
  #         - not_null
  #     - name: status
  #       tests:
  #         - accepted_values:
  #             values:
  #               - completed
  #               - shipped
  #               - returned
  #               - placed
  #               - return_pending
